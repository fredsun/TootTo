<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="preferenceTheme">@style/PreferenceThemeOverlay.v14.Material</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!--<item name="android:windowIsTranslucent">true</item>-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="tab_icon_selected_tint">@color/colorGrassGreen</item>
        <item name="tab_icon_unselected_tint">@color/colorIconUnselected</item>
        <item name="status_favourite_active_drawable">@drawable/ic_favorite_pink_24dp</item>
        <item name="status_favourite_inactive_drawable">@drawable/ic_favorite_border_black_24dp</item>
    </style>

    <style name="AppTheme.Account.AppBarLayout" parent="ThemeOverlay.AppCompat">
        <item name="android:textColorPrimary">@color/colorShineWhite</item>
        <item name="android:textColorSecondary">@color/foreground_window_light</item>
        <item name="android:textColorTertiary">@color/colorBackground</item>
    </style>

    <style name="AppTheme.Account.ToolbarPopupTheme.Dark" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/colorShineWhite</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>



</resources>
